/** Site-wide URLs and handles (non-translated). */
export const SITE = {
  /** Must match `site` in astro.config.mjs */
  url: 'https://tudominio.com',
  ogImage: '/og-image.jpg',
  twitterHandle: '@tuusuario',
} as const;
